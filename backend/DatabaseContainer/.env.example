# PostgreSQL container configuration (example values; do NOT commit real secrets)
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# Application DB and user (created by init script)
APP_DB_NAME=drm_app
APP_DB_USER=drm_user
APP_DB_PASSWORD=drm_password
